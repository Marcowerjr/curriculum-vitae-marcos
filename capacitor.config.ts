import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'curriculum-vitae-marcos',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
